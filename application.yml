spring:
  security:
    oauth2:
      client:
        registration:
          github:
#            client-id: replace this with your own id and secret
#            client-secret: if this is still here you copy pasted
          google:
            client-id: your-google-client-id
            client-secret: your-google-client-secret

# logging:
#  level:
#    org.springframework.security: DEBUG
